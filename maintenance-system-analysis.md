
# تحليل نظام إدارة طلبات الصيانة للمهندسين

## 1️⃣ الهدف من النظام

تحويل نموذج طلبات الصيانة الورقي إلى نظام إلكتروني يربط بين:

- **الإدارة (Admin)**
- **المكتب الاستشاري (Consultants)**
- **المهندسين (Engineers)**

بحيث يقوم النظام بـ:

- تمكين المهندس من تقديم طلب صيانة، متابعة حالته، وإغلاق الطلب بعد التنفيذ.
- تمكين الاستشاري من مراجعة الطلبات، إضافة الملاحظات، والموافقة على التنفيذ.
- تمكين الأدمن من إدارة المستخدمين والقوائم المرجعية والتقارير العامة.

---

## 2️⃣ الأدوار والصلاحيات

### 👑 1. الأدمن (Admin)

**الصلاحيات:**

- إدارة حسابات المستخدمين:
  - إنشاء / تعديل / إيقاف حسابات المهندسين.
  - إنشاء / تعديل / إيقاف حسابات الاستشاريين.
- إدارة القوائم المرجعية:
  - المواقع (كلية الحاسوب، كلية الهندسة، الورش، الإسكان…).
  - الأقسام (ميكانيك، كهرباء).
  - الأنظمة (تكييف، حريق، مياه…).
  - الآلات المرتبطة بكل نظام (تشيلر، AHU، مضخات…).
- مشاهدة جميع الطلبات مع إمكانيات الفلترة حسب:
  - المهندس، الاستشاري، حالة الطلب، القسم، الموقع، نوع الصيانة…
- تصدير تقارير عامة (Excel / PDF) حسب:
  - الفترة الزمنية.
  - القسم / الموقع.
  - نوع الصيانة.

---

### 🧠 2. الاستشاري (Consultant)

**الصلاحيات:**

- مشاهدة **كل الطلبات** في النظام.
- فلترة الطلبات حسب:
  - المهندس، الحالة، التاريخ، نوع الصيانة، الموقع، القسم…
- مراجعة الطلبات في حالة: **"قيد المراجعة"**.
- اتخاذ إجراء على الطلب:
  - **الموافقة** → يتحول إلى: **"قيد التنفيذ"**.
  - **الرفض أو طلب تعديل** (حسب سياسة العمل المتفق عليها).
- إضافة:
  - **ملاحظات المكتب الاستشاري** (حقل خاص يظهر للمهندس للقراءة).
- مشاهدة الإحصائيات:
  - عدد طلبات كل مهندس.
  - عدد الطلبات حسب الحالات (قيد المراجعة، قيد التنفيذ، منتهية).
  - عدد الطلبات حسب نوع الصيانة (طارئة / وقائية).
- تصدير تقارير لكل مهندس لفترة معيّنة:
  - من تاريخ إلى تاريخ.
  - مع عدد الطلبات وحالاتها وأنواعها.

---

### 🧑‍🔧 3. المهندس (Engineer)

**الصلاحيات:**

- تسجيل الدخول إلى لوحة خاصة به.
- إنشاء طلب صيانة جديد يحتوي على الحقول التالية:

1. **معرّف المهندس**: يحدد تلقائيًا من حسابه.
2. **نوع الصيانة**: طارئة / وقائية.
3. **الموقع**: اختيار من قائمة (كلية الحاسوب، كلية الهندسة، الورش، الإسكان…).
4. **القسم**: ميكانيك / كهرباء.
5. **النظام**: تكييف / حريق / مياه (من قائمة).
6. **الآلة**: قائمة ديناميكية مرتبطة بالنظام المختار  
   (إذا كان النظام تكييف → تظهر آلات مرتبطة مثل: تشيلر، AHU… يتم ربطها من لوحة التحكم).
7. **سبب طلب الصيانة**: إدخال نصي (وصف المشكلة).
8. **رقم الآلة**: إدخال نصي.
9. **تاريخ فتح الطلب**: يُسجّل تلقائيًا عند إنشاء الطلب.
10. **تاريخ إغلاق الطلب**: يُسجّل تلقائيًا عند إغلاق الطلب من قبل المهندس.
11. **ملاحظات المهندس**: إدخال نصي.
12. **ملاحظات المكتب الاستشاري**: حقل للقراءة يظهر بعد مراجعة الاستشاري (لا يمكن للمهندس تعديله).

- متابعة حالة طلباته:
  - **قيد المراجعة**.
  - **قيد التنفيذ**.
  - **منتهي**.
- إغلاق الطلب بعد الانتهاء من التنفيذ:
  - تغيير الحالة إلى **"منتهي"**.
  - تسجيل **تاريخ الإغلاق** تلقائيًا.

---

## 3️⃣ نموذج البيانات (Entities) – بشكل مبسّط

### 👤 جدول المستخدمين (Users)

- `id`
- `name`
- `email` / `username`
- `password` (مشفّر)
- `role`: `admin | consultant | engineer`
- `departmentId` (اختياري – يرتبط بالأقسام للمهندسين)
- `isActive`

---

### 🏢 جدول المواقع (Locations)

- `id`
- `name` (مثل: كلية الحاسوب، كلية الهندسة…)
- `description` (اختياري)
- `isActive`

---

### 🧩 جدول الأقسام (Departments)

- `id`
- `name` (ميكانيك، كهرباء)
- `isActive`

---

### ⚙️ جدول الأنظمة (Systems)

- `id`
- `name` (تكييف، حريق، مياه)
- `description` (اختياري)
- `isActive`

---

### 🛠️ جدول الآلات (Machines)

- `id`
- `systemId` (ربط الآلة بالنظام)
- `name` (تشيلر، AHU، مضخة…)
- `description` (اختياري)
- `isActive`

---

### 📄 جدول طلبات الصيانة (MaintenanceRequests)

الحقول الأساسية:

- `id`
- `requestCode` (رقم طلب تسلسلي – اختياري لكنه مفيد)
- `engineerId` (من جدول Users)
- `consultantId` (آخر استشاري راجع الطلب – اختياري)
- `maintenanceType`: `emergency | preventive` (طارئة / وقائية)
- `locationId` (من Locations)
- `departmentId` (من Departments)
- `systemId` (من Systems)
- `machineId` (من Machines)
- `reasonText` (سبب طلب الصيانة)
- `machineNumber` (رقم الآلة – إدخال نصي)
- `status`:
  - `pending_review` = قيد المراجعة
  - `in_progress` = قيد التنفيذ
  - `completed` = منتهي
  - (إمكانية إضافة حالة مثل: `rejected` لاحقًا)
- `engineerNotes` (ملاحظات المهندس)
- `consultantNotes` (ملاحظات المكتب الاستشاري)
- `openedAt` (تاريخ فتح الطلب – تلقائي)
- `closedAt` (تاريخ الإغلاق – تلقائي عند الإنهاء)
- `createdAt`
- `updatedAt`

---

## 4️⃣ دورة حياة الطلب (Workflow)

1. **إنشاء الطلب (المهندس):**
   - يدخل المهندس إلى النظام.
   - يختار: نوع الصيانة، الموقع، القسم، النظام، الآلة…
   - يضيف سبب الطلب ورقم الآلة وملاحظاته.
   - يحفظ الطلب → الحالة تصبح: **قيد المراجعة**، ويُسجّل `openedAt` تلقائيًا.

2. **مراجعة الطلب (الاستشاري):**
   - يرى الطلب ضمن قائمة "الطلبات قيد المراجعة".
   - يفتح الطلب ويطلع على المعلومات.
   - يضيف **ملاحظات المكتب الاستشاري**.
   - يقرّر:
     - **الموافقة** → الحالة تتحول إلى: **قيد التنفيذ**.
     - أو **الرفض / الإرجاع للتعديل** (إذا تم اعتماد هذه الحالة لاحقًا).

3. **تنفيذ وإغلاق الطلب (المهندس):**
   - يرى الطلب في قائمة "قيد التنفيذ".
   - ينفّذ الأعمال المطلوبة على أرض الواقع.
   - بعد الانتهاء، يغيّر الحالة إلى: **منتهي**.
   - النظام يسجّل تلقائيًا `closedAt` (تاريخ الإغلاق).

---

## 5️⃣ التقارير والإحصائيات

### تقارير الاستشاري:

- عدد طلبات كل مهندس في فترة معيّنة.
- عدد الطلبات حسب نوع الصيانة:
  - طارئة / وقائية.
- عدد الطلبات حسب:
  - الموقع.
  - القسم.
  - حالة الطلب.

### تقارير الأدمن (Admin):

- كل تقارير الاستشاري +
- تقارير شاملة مثل:
  - أكثر الآلات تعطلًا.
  - أكثر المواقع طلبًا للصيانة.
  - أكثر الأقسام طلبًا للصيانة.

> **ملاحظة:** يمكن تصدير التقارير بصيغ مثل: **Excel** أو **PDF**.

---

## 6️⃣ خطوات تطوير النسخة الأولى (MVP)

1. **تعريف الأدوار والصلاحيات (Roles & Permissions).**
2. **بناء قاعدة البيانات** وفق الجداول المذكورة أعلاه.
3. **تطوير الواجهات الأساسية:**
   - شاشة تسجيل الدخول.
   - لوحة المهندس:
     - إنشاء طلب.
     - قائمة طلباته مع فلترة بسيطة.
   - لوحة الاستشاري:
     - قائمة الطلبات حسب الحالة.
     - شاشة مراجعة الطلب (تعديل الحالة + إضافة الملاحظات).
   - لوحة الأدمن:
     - إدارة المستخدمين.
     - إدارة القوائم (مواقع – أنظمة – آلات – أقسام).
4. **إضافة شاشة تقارير أولية:**
   - تقرير بعدد طلبات كل مهندس في فترة محددة.

---

بهذا الملف تقدر تستخدمه كأساس:
- لشرح الفكرة للعميل.
- أو لتحويله مباشرة إلى User Stories / ERD / API Design في المرحلة التالية.
